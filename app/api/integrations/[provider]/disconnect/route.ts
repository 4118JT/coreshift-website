import { asProvider, disconnectProvider, requireOwner } from "../../_shared";
export async function DELETE(request: Request, context: { params: Promise<{ provider: string }> }) { const viewer = await requireOwner(); if (!viewer) return Response.json({ error: "Owner access required" }, { status: 403 }); const provider = asProvider((await context.params).provider); if (!provider) return Response.json({ error: "Unknown integration provider" }, { status: 404 }); await disconnectProvider(viewer.businessId, provider, new URL(request.url).origin); return Response.json({ disconnected: true }); }
